
export interface Options {
  data?: string;
  charset?: string;
  labels?: boolean;
  quote?: string;
  separator?: string;
  CRLF?: string;
}

export interface CSS {
  table?: string;
  th?: string;
}
